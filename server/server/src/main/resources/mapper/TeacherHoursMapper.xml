<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.server.mapper.ReportMapper">
    <select id="calculateTeacherHours" statementType="CALLABLE" resultType="com.example.server.model.TeacherHoursDTO">
        {CALL CalculateTeacherHours(
                #{startDate, mode=IN, jdbcType=DATE},
                #{endDate, mode=IN, jdbcType=DATE}
              )}
    </select>
</mapper>